using System.IO;

namespace Collector.Formats.Tapes.Atari
{
    public class CasFileReader
    {
        public CasFile Read(string filename)
        {
            var casFile = new CasFile();
            File.ReadAllBytes()

            return casFile;
        }
    }
}
